# 复审报告 / Re-audit (2026-06-04, post-remediation)

> 8 维度并行 finder → 每条对抗式复核（43 agents，2.57M tokens）。49 条发现，复核驳回 2，**47 确认**，去重后 **32** 项：**15 个 bug + 17 个优化**。仅报告。

> ⚠️ 其中 2 个 bug 是上一轮修复引入的回归（H4 指标 label、DB 双写），1 个 CRITICAL 是首轮审计漏掉的（WeChat IDOR）。


---

## Bugs / 缺陷


### CRITICAL (1)

**CRITICAL-1. WeChat channel never binds a caller identity → IDOR/BOLA on every order tool for all WeChat users**  _(security_residual)_
- 位置: `src/open_chat_shop/api/wechat.py` @ 185-189
- 问题: receive_message builds UserMessage(session_id=from_user, content=content, channel="wechat") with NO user_id. The IDOR remediation wired the verified identity onto context.user_id only for the REST (app.py:317) and WebSocket (app.py:434) entries; the WeChat webhook was left out. Because user_id is None, the orchestrator's step-2.05 binding (orchestrator.py:243-257) is skipped, so context.user_id stays None and OrderRepository.get_for_user (abc.py:57: `if user_id is not None and owner is not None`) returns the order regardless of owner. Every WeChat user can therefore query_order / query_logisti
- 影响: The signed WeChat POST authenticates the sender's OpenID in from_user, but it is used only as session_id, never as the ownership identity. The remediation's own design (ownership enforced via context.user_id) is bypassed end-to-end for WeChat, re-opening cross-tenant read AND irreversible write access (refund/cancel) to other customers' orders.
- 建议: Derive the ownership identity for WeChat from the authenticated from_user (e.g. map OpenID→customer_id, or set UserMessage.user_id=from_user if orders are keyed by OpenID) so context.user_id is populated and get_for_user engages. If WeChat OpenIDs are not order owners, gate order tools off entirely for that channel until a mapping exists. Add a regression test that a WeChat user cannot read an ord
- 复核: VERIFIED REAL — the WeChat channel re-opens the exact IDOR/BOLA the audit claimed fixed, on every order tool, for every WeChat sender. I traced the full enforcement chain:

ENFORCEMENT IS KEYED SOLELY ON context.user_id: All 7 order tools c…


### HIGH (3)

**HIGH-1. DB double-persists assistant turn (regression: _record_turn + db save both append)**  _(reported ×6 across architecture, correctness_deep, performance, regression_from_fixes, security_residual)_
- 位置: `src/open_chat_shop/storage/db_context.py` @ 301-322 (with src/open_chat_shop/core/orchestrator.py:403-404)
- 问题: Two independently-developed fixes contradict each other. The new orchestrator `_record_turn` (orchestrator.py:403, also :300/:330/:353) appends BOTH the user turn AND the assistant `response` into `context.history`, THEN calls `save(context, response)`. The rewritten DB backend `_save_sync` writes every row in `context.history` (db_context.py:301-310) AND THEN appends the same `response` AGAIN as a separate assistant row (db_context.py:312-322). Net effect on the DB backend: the assistant reply is stored twice, and on reload the history shows duplicated assistant turns. InMemory/Redis `save` d
- 影响: Proven with a direct repro: feeding a context whose history already ends in the assistant turn (the orchestrator's actual state at save time) into `DatabaseContextManager.save(ctx, reply)` produced history `[user, assistant, assistant]` on reload (assistant-turn count = 2). The duplicate compounds every turn (each save re-writes the now-duplicated history rows plus a fresh duplicate), and it pollu
- 建议: Pick ONE owner of the assistant-turn append. Either (a) have the DB backend persist only `context.history` and stop independently appending `response` (since `_record_turn` already put it there) — mirroring InMemory/Redis; or (b) keep the DB backend appending `response` and have the orchestrator's `_record_turn` append ONLY the user turn, letting each backend add the assistant turn. Option (a) mak
- 复核: CONFIRMED REAL via direct repro. Two fixes contradict each other across an untested seam.

Root cause (contract asymmetry):
- orchestrator._record_turn (orchestrator.py:1057-1060) appends BOTH the user turn AND the assistant reply (Message(…

**HIGH-2. Cache hits double-bill token usage (DB backend)**  _(regression_from_fixes)_
- 位置: `src/open_chat_shop/storage/db_context.py` @ 191-192 (with src/open_chat_shop/core/orchestrator.py:347-354)
- 问题: On a cache hit the orchestrator still calls `_record_turn(context, message, cached)` and `save(context, cached)` (orchestrator.py:353-354). The in-memory ResponseCache stores the full AgentMessage object by reference INCLUDING `meta['token_usage']` (cache.py:148; note the Redis path drops meta at cache.py:139-147). The DB backend's save then folds `response.meta['token_usage']` into cumulative usage unconditionally: `response_tokens = int(response.meta.get('token_usage', 0) or 0)` then `cumulative_tokens = context.token_usage + response_tokens` (db_context.py:191-192). A cache hit performs ZER
- 影响: Proven with a repro against DatabaseContextManager: a first real turn recorded token_usage=100, then a simulated cache-hit save of a response still carrying meta token_usage=100 (no real LLM call) raised cumulative token_usage to 200 instead of staying ~100. This over-counts cost/budget for cached read-only intents (order/logistics cards) — exactly the responses most likely to be cached and repeat
- 建议: Do not bill tokens for cache hits. Either strip/zero `meta['token_usage']` on the cached object before `save` on the cache-hit branch, or have the orchestrator pass an explicit 'no new tokens' signal so the DB backend folds in 0 for served-from-cache turns. Add a test covering cache-hit -> DB-save asserting cumulative token_usage is unchanged.
- 复核: CONFIRMED REAL and reachable. Reproduced end-to-end against the real DatabaseContextManager + real in-memory ResponseCache: a first real 100-token turn yields cumulative=100, then two zero-LLM cache hits raise it to 200 then 300 — each hit …

**HIGH-3. Streaming 'done' consumers ignore the new text_fallback**  _(api_ops_dx)_
- 位置: `src/open_chat_shop/api/app.py` @ 386-388 (SSE), 512-514 + 533 (WebSocket)
- 问题: streaming.py was remediated to carry text_fallback EXPLICITLY in the done event (streaming.py:88) with a docstring (lines 78-82) stating: 'text_fallback is carried explicitly so downstream reconstruction does NOT have to dig it out of payload["content"] — rich payloads (order_card, product_list, ...) have no "content" key, so relying on payload would collapse the fallback to "" and lose the tool's computed text'. But BOTH consumers in app.py were never updated to read it: the SSE path (lines 386-388) and the WebSocket path (lines 512-514) both reconstruct `text_fallback=event.data.get("payload
- 影响: The remediation added the field to the producer but left both consumers (and the history recorder) reading the old path, so the fix is entirely dead and the original bug persists on a normal path. Impact is user-visible: empty replies on downgrade and blank assistant messages in the human-agent dashboard history, defeating the 'send accumulated history so the agent has context' design.
- 建议: In both app.py done branches reconstruct as `text_fallback=event.data.get("text_fallback") or event.data.get("payload", {}).get("content", "")`. Likewise store the history `content` from `event.data.get("text_fallback")` with the payload-content fallback. Add a streaming-integration test that pushes an order_card through a text-only channel adapter and asserts the downgraded done frame's text is n
- 复核: CONFIRMED REAL, not handled, reachable on normal paths. HIGH stands.

Producer fix is live but dead-on-arrival because both consumers ignore it:
- streaming.py:88 puts text_fallback explicitly into the done event; docstring at streaming.py:…


### MEDIUM (8)

**MEDIUM-1. Unbounded in-process write-through _cache in Redis/DB context managers**  _(regression_from_fixes)_
- 位置: `src/open_chat_shop/storage/redis_context.py` @ 102 (and src/open_chat_shop/storage/db_context.py:104)
- 问题: The remediation added a sync `get()` backed by an in-process write-through cache `self._cache: dict[str, SessionContext]` to both RedisContextManager (redis_context.py:102, written at :129/:132/:151) and DatabaseContextManager (db_context.py:104, written at :113/:221). Neither dict has ANY eviction, TTL, or size cap — every distinct session_id ever loaded or saved adds a permanent entry holding a full SessionContext (history + slots + summary). This is heavier than the lock/timestamp entries the SAME remediation bothered to cap (orchestrator `_session_locks` cap=10000 at orchestrator.py:166-17
- 影响: In a long-running multi-tenant process this grows by one heavyweight SessionContext per unique session forever, leading to steadily climbing RSS and eventual OOM — the exact failure mode the remediation guarded against for locks and rate-limit keys, reintroduced via the new caches. The staleness of `get()` is tolerable (callers read only `ctx.mode` for a HUMAN_MODE guard with a parallel `_session_
- 建议: Bound both `_cache` dicts: use an LRU (OrderedDict with move_to_end + popitem(last=False)) capped at a few thousand entries, or evict on a size threshold mirroring `_session_locks`. A small cache is sufficient because it only needs to answer the sync `get()` for the in-flight request; the DB/Redis remains source of truth.
- 复核: CONFIRMED REAL (not a false positive). Both write-through caches are genuinely unbounded.

Evidence read:
- redis_context.py:102 declares `self._cache: dict[str, SessionContext] = {}`; written on load at :129 and :132, on save at :151; read…

**MEDIUM-2. ACTIVE_SESSIONS gauge tracks lock-table size, not real concurrency**  _(reported ×3 across api_ops_dx, architecture, regression_from_fixes)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 177 (set), 81 (cap), 166-174 (eviction)
- 问题: ACTIVE_SESSIONS.set(len(self._session_locks)) reports the size of the per-session asyncio.Lock dict. That dict only grows (one entry per session_id ever seen) and is pruned only when it EXCEEDS _SESSION_LOCKS_CAP=10000, and even then only idle locks are removed down toward the cap. There is no decrement when a request finishes or a session goes idle. So the gauge ratchets up to ~10000 and stays there regardless of how many sessions are genuinely active right now. It measures 'distinct sessions seen since last eviction', not active sessions.
- 影响: metrics.py documents it as 'Number of currently active sessions' and README lists it as '活跃会话数'. An operator watching this gauge for load/capacity will be badly misled — it never reflects current concurrency and pins near the cap. This is an observability-correctness gap surfaced by the H4 wiring (the gauge was wired to a convenient-but-wrong source).
- 建议: Either rename/redocument it as a saturation gauge (e.g. openchatshop_session_lock_table_size) OR set it from a real in-flight counter incremented on entry / decremented in the finally (which already exists at lines 175-195). The simplest correct signal: track a counter of currently-held locks and gauge that.
- 复核: CONFIRMED REAL. Read orchestrator.py:80-81, 156-195, and metrics.py:119-122, README.md:759.

Mechanics verified:
- orchestrator.py:80 `_session_locks: dict[str, asyncio.Lock] = {}` — keyed by session_id.
- orchestrator.py:174 `setdefault(me…

**MEDIUM-3. H4 chat metrics intent label always 'unknown' (wrong meta key: reads 'intent', set as 'intent_name')**  _(reported ×5 across api_ops_dx, correctness_deep, performance, security_residual, tests_coverage)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 186 (vs 386)
- 问题: handle_message's finally block builds the metric label via response.meta.get('intent') (line 186), but the orchestrator only ever writes the intent under response.meta['intent_name'] (line 386). 'intent' is never set anywhere in core (grep confirms only 'intent_name'). So record_chat_request(label, status) and observe_chat_duration(label, ...) always receive label='unknown'. CHAT_REQUESTS_TOTAL / CHAT_DURATION_SECONDS are declared with an ['intent'] label (metrics.py:101,111) specifically to break down by intent.
- 影响: This is in the H4 metrics call-site wiring that the remediation added (metrics +93). The entire per-intent dimension of the request-count and latency histograms is silently collapsed to 'unknown', so the observability the fix was meant to deliver is non-functional. No test catches it because metrics are no-op in test.
- 建议: Read response.meta.get('intent_name') (or set both keys). Trivial one-line fix: change line 186 to str(response.meta.get('intent_name') or 'unknown').
- 复核: CONFIRMED real and reachable. orchestrator.py:186 builds the metric label via `str(response.meta.get("intent") or "unknown")`, but the orchestrator's only write of the resolved intent into response.meta is at line 384-390, key "intent_name"…

**MEDIUM-4. Human-handoff auto-assign can connect the wrong session and strand the caller in HUMAN_MODE**  _(correctness_deep)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 663-676 (with handoff.py:248-260)
- 问题: The transfer case enqueues the current session's request then calls try_auto_assign(), which assigns self._queue[0] — the highest-priority / oldest queued request (handoff.py:256). enqueue() stable-sorts by priority (handoff.py:126) and all orchestrator-built requests use priority=0, so the just-enqueued request lands at the BACK of any existing backlog. If the queue already held another session's request and an agent is free, try_auto_assign assigns THAT other session, yet the orchestrator unconditionally sets the CURRENT session's context.mode=HUMAN_MODE and human_agent_id=agent_id and repli
- 影响: The current caller is told a human is connected and the bot goes silent (HUMAN_MODE), but no agent is actually assigned to that session — the user is stranded with no bot and no human. Meanwhile the genuinely-assigned session (A) gets no mode update from this call. Requires a non-empty queue plus a free agent simultaneously, so it's a backlog edge case, hence MEDIUM.
- 建议: Use the return value correctly: only treat it as success when assigned.session_id == context.session_id, otherwise keep the current session in TRANSFER_PENDING with its real queue position. Better: add a HandoffQueue.assign_session(session_id) that assigns the specific request rather than try_auto_assign() picking queue head.
- 复核: CONFIRMED REAL. Mechanism verified by reading the code: (1) orchestrator.py:656-663 builds TransferRequest without setting priority, so it defaults to priority=0 (handoff.py:78). enqueue (handoff.py:125-126) does append() then a stable sort…

**MEDIUM-5. High-risk confirmation accepts non-consent replies as affirmative (substring match on 确认/继续/同意)**  _(security_residual)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 415-444
- 问题: _AFFIRM_RE matches 确认/确定/同意/没错/执行/继续 as an UNANCHORED substring anywhere in the reply. The question-guard (_QUESTION_RE) only catches replies containing 吗/呢/哪/？ etc. Reproduced false-affirmatives that execute the irreversible write: "请帮我确认一下订单" → affirm, "继续执行删除" → affirm, "同意退款500元" → affirm, "我要确认退款" → affirm. So when a create_refund(>500)/cancel_order/modify_address confirmation is pending, a user message that merely mentions 确认/继续/同意 (without being explicit consent and without a question marker) triggers the pending tool_call via _resolve_pending_confirmation (orchestrator.py:464-488).
- 影响: The HIGH-9 fix tightened only the interrogative case (确认吗?); it did not anchor the affirmation tokens, so clarifying or compound utterances that contain an affirmation word as a substring still fire the irreversible operation. This is the same accidental-write risk the fix targeted, on a different phrasing.
- 建议: Anchor the multi-char affirmations the way the single-char group already is (e.g. require the whole stripped reply to be a confirmation, or that 确认/确定/同意/继续 stand alone / are not embedded in a longer instruction), and/or treat replies longer than a short threshold as non-consent. Add the reproduced phrases as regression cases asserting they do NOT execute the pending write.
- 复核: CONFIRMED REAL (verified by executing the detector, not just reading). In src/open_chat_shop/core/orchestrator.py:415-419 `_AFFIRM_RE` has two top-level alternatives split by `|`: the FIRST `^(好|好的|是|...|行)$` is anchored, but the SECOND gro…

**MEDIUM-6. compress() is never invoked — unbounded in-memory/Redis history growth + per-turn cost cliff**  _(performance)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 403
- 问题: _record_turn appends two Messages to context.history every turn (orchestrator.py:1057-1060), and this is now the sole growth path for InMemory/Redis backends. But compress() — defined on all three context managers (context.py:137, redis_context.py:164, db_context.py:329) and gated by needs_compression — is never called anywhere in src/ (verified: only the tracing span name 'context.compress' and the method defs match). So history grows by 2 messages per turn forever for a given session.
- 影响: For long-lived sessions: (1) memory/Redis hash size grows without bound; (2) every turn pays O(history) for the InMemory save copy [*context.history] (context.py:122), the Redis JSON serialize of the full history (redis_context.py:33-36), get_token_budget's full re-sum (per turn), and _build_history_text's slice. The token budget machinery (the whole point of compress) is computed but its result i
- 建议: Wire compaction into the save path or the orchestrator after _record_turn: if get_token_budget(context).needs_compression, replace context with await context_manager.compress(context) before/within save. Alternatively cap context.history to the last N turns in _record_turn (e.g. keep last 12 messages) since only history[-6:] (LLM) and history[-3:] (intent) are ever read. Add a test asserting histo
- 复核: VERIFIED REAL but severity overstated. Core claim confirmed: compress() is never called from src/. `grep -rn "\.compress("` across src+tests returns matches ONLY in tests/unit/test_context.py, test_redis_context.py, test_db_context.py — nev…

**MEDIUM-7. Retry-layer fix only covers AnthropicProvider; the LiteLLM fallback path still raises non-retryable ProviderError**  _(architecture)_
- 位置: `src/open_chat_shop/core/litellm_provider.py` @ 182-186 (_wrap_error); contrast provider.py 23-35 + anthropic_provider.py 120
- 问题: The PROVIDER-HIGH audit fix introduced TransientProviderError(ProviderError, TimeoutError) so RetryPolicy._RETRYABLE matches wrapped transient failures, and AnthropicProvider._wrap_error correctly emits it for transient upstream errors (anthropic_provider.py line 120). But LiteLLMProvider._wrap_error unconditionally returns a plain ProviderError for EVERY exception (timeouts, 5xx, rate limits included). main.py _build_provider documents LiteLLM as the fallback provider, and main.py wraps provider.chat with retry_policy.execute regardless of which provider was selected — so on a LiteLLM deploym
- 影响: The remediation note claims the retry layer is no longer 'dead for production traffic', but that only holds for the Anthropic path. Any deployment that falls through to LiteLLM (the explicit documented fallback) gets zero retry on genuinely transient failures, so the resilience guarantee is provider-dependent and silently incomplete.
- 建议: Mirror the Anthropic fix in LiteLLMProvider._wrap_error: classify timeouts/connection/5xx/429 (e.g. via litellm.Timeout/APIConnectionError/RateLimitError or HTTP status) and raise TransientProviderError for those, keeping auth/bad-request as plain ProviderError. Add a unit test asserting a simulated LiteLLM timeout surfaces as TransientProviderError and is retried.
- 复核: VERIFIED REAL. The behavior chain is airtight end-to-end:

1. main.py:120-138 `_build_provider()` returns `LiteLLMProvider(model="gpt-4o-mini")` as the documented fallback whenever `AnthropicProvider()` is unavailable. Anthropic is unavaila…

**MEDIUM-8. WeChat 5s deadline cancels the in-flight turn mid-execution; a tool write that already committed is then hidden behind the dedup empty-ack**  _(architecture)_
- 位置: `src/open_chat_shop/api/wechat.py` @ 194-215
- 问题: receive_message wraps handle_message in asyncio.wait_for(timeout=4.5s). On timeout, wait_for CANCELS the orchestrator coroutine (which holds the per-session asyncio.Lock) and the handler records the MsgId with an EMPTY ack so WeChat stops retrying. But cancellation can land AFTER a side-effecting tool already ran (e.g. create_refund/cancel_order executed in _execute_tool, then the slow part is the LLM text enhancement at lines 856/870). The orchestrator's compensation only runs inside `except ToolError` — a CancelledError raised during _llm_enhance_tool_result does NOT trigger compensate, so t
- 影响: This is a write-visibility / partial-commit hazard specific to the channel-timeout fix combined with the idempotency cache: an irreversible action can complete while the user receives silence, and the cached empty-ack suppresses the natural retry that would have surfaced it. Low frequency (only when a write turn straddles 4.5s) but high consequence (silent refund/cancel).
- 建议: Cache the FINAL reply (not an empty ack) keyed by MsgId only after the turn fully completes; on timeout, do NOT poison the cache with an empty ack for write intents — let WeChat retry hit the idempotent in-flight result, or return a 'processing, please check shortly' text. Alternatively run handle_message detached and have the timeout only abandon the HTTP wait, not cancel the orchestrator, so the
- 复核: VERIFIED REAL. The mechanism chain holds end-to-end against the actual code:

1) Timeout cancels the live turn. wechat.py:194-198 wraps handle_message in asyncio.wait_for(timeout=4.5s), which cancels the inner coroutine on expiry.

2) Cance…


### LOW (3)

**LOW-1. SSE and WebSocket 'done' reconstruction is duplicated, and rebuilds text_fallback from payload['content'] — contradicting the streaming.py remediation**  _(simplification)_
- 位置: `src/open_chat_shop/api/app.py` @ 383-401, 509-529
- 问题: chat_stream (383-401) and websocket_chat (509-529) contain near-identical blocks that rebuild an AgentMessage from the done event and re-adapt it. Both set `text_fallback=event.data.get('payload', {}).get('content', '')`. But streaming.py was just remediated (streaming.py:83-92, with an explicit comment) to put `response.text_fallback` directly into the done event's data, precisely because rich payloads (order_card/product_list/...) have NO 'content' key so pulling text_fallback from payload['content'] collapses it to ''. These callers never adopted the new `text_fallback` field, so for a down
- 影响: On the default web channel this is currently latent, not live data-loss: WebAdapter supports all 11 types (web.py:17-29) so adapt_with_fallback takes the adapt() branch and the reconstructed text_fallback is never read, and the done frame doesn't re-emit text_fallback to the client anyway. But it becomes a real empty-bubble the moment a capability-limited channel (e.g. WeChat) drives the WS/SSE pa
- 建议: In both blocks use `text_fallback=event.data.get('text_fallback', '')` (now provided by streaming.py), and factor the shared done→AgentMessage→adapt→frame logic into one helper used by both chat_stream and websocket_chat.
- 复核: LOW — not adversarially verified…

**LOW-2. The full 500-sample regression + intent_accuracy gate is never run by any pytest test — only by the manual CLI**  _(tests_coverage)_
- 位置: `src/open_chat_shop/evaluation/__main__.py` @ 39-137 (_run_regression / intent_accuracy gate); only tested by tests/unit/test_evaluation_cli.py:56-111
- 问题: _run_regression runs the orchestrator over all 500 golden samples and gates on intent_accuracy >= EVAL_MIN_INTENT_ACCURACY (default 0.6) plus the attack zero-tool security gate. But NO pytest test invokes this end-to-end. test_evaluation_cli.py only: (a) runs the 'list' subcommand as a subprocess, (b) tests RegressionRunner SCORING on a hand-built tuple with a pre-filled intent (test_regression_scoring_on_result_tuple — its own docstring at lines 57-62 says it does NOT run the orchestrator and cannot catch an extraction break), and (c) asserts a single sample (BO-001) records intent_name on me
- 影响: The dimension explicitly asks 'whether the eval gate truly protects quality.' It does not in automated CI: the gate's teeth (intent_accuracy over 500 samples) are exercised only manually. The 1159-test green suite gives a false sense that intent quality is guarded.
- 建议: Add a pytest test that calls _run_regression() (or runs the orchestrator over a representative dataset slice) and asserts intent_accuracy >= the gate, so an intent-classification regression turns the suite red. Even a subset (the edge+attack samples plus N normal) executed through the real orchestrator would restore protection without the cost of all 500.
- 复核: PARTLY REAL but OVERSTATED — adjusted MEDIUM→LOW. The defensible core holds: no pytest exercises the intent_accuracy quality gate over the dataset. I grepped all of tests/ for _run_regression/intent_accuracy/EVAL_MIN_INTENT/min_intent — the…

**LOW-3. README test-count badge and prose are stale (898 vs actual 1159)**  _(api_ops_dx)_
- 位置: `README.md` @ 14, 610, 981
- 问题: Three places claim 898 tests: the badge `Tests-898 passing` (line 14), '898 个现有测试零修改通过' (line 610), and '测试（898 个用例）' (line 981). `pytest --co` collects 1159 tests (the harness context also states 1159). The remediation added ~261 tests (incl. tests/unit/test_agent_auth.py from this branch) without updating the README.
- 影响: Developer-experience / trust signal. A README that under-reports the suite by 30% undercuts confidence and the test badge is the first thing a contributor sees. Low impact but trivially fixable and a direct DX accuracy miss.
- 建议: Update the badge and the two prose mentions to 1159 (or, better, generate the badge count in CI so it cannot drift again).
- 复核: LOW — not adversarially verified…


---

## Optimizations / 优化


### MEDIUM (7)

**MEDIUM-1. orchestrator.py (1068) / app.py (796) oversized — extract cohesive modules**  _(reported ×4 across api_ops_dx, architecture, regression_from_fixes, simplification)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 61-136 (5 ctor deps + 8 set_* setters), whole file 1068 lines
- 问题: DialogueOrchestrator wires 13 collaborators (security, context_manager, intent_engine, tool_injector, strategy via ctor; provider, audit_logger, cost_tracker, tool_response_mapper, scenarios, handoff_queue, middleware_pipeline, response_cache via 8 setters) and owns at least five distinct responsibilities in one class: (a) request pipeline (security->context->intent->cache->inject->strategy->execute), (b) the high-risk confirmation FSM (_detect_affirmation + regex consts + _resolve_pending_confirmation, lines 408-501), (c) pending-slot recovery (_try_resolve_pending/_slot_prompt, 503-608), (d)
- 影响: Every new cross-cutting concern grows this file and its setter list; the confirmation-FSM and slot-recovery sub-machines are independently testable units fused into the request path, making the core handler hard to read and reason about. This is the central extensibility bottleneck the user is asking about ('哪里还能优化').
- 建议: Extract the confirmation/affirmation machine (regexes + _detect_affirmation + _resolve_pending_confirmation) and the pending-slot recovery (_try_resolve_pending/_slot_prompt) into two small collaborators (e.g. ConfirmationResolver, PendingSlotResolver) that the orchestrator delegates to. Replace the 8 free-floating `Any` setters with a typed Collaborators dataclass passed once, so the wiring contr
- 复核: VERIFIED REAL (architecture/quality, not a bug). Facts all check out against the code:

- orchestrator.py is exactly 1068 lines (`wc -l`), the single largest file in src/ (next is app.py at 796); both exceed the project's own "800 max / 200…

**MEDIUM-2. Scenario FSM subsystem unwired at runtime (dead surface)**  _(reported ×2 across correctness_deep, simplification)_
- 位置: `src/open_chat_shop/core/scenario.py` @ 38-93 (plus scenarios/order_inquiry.py, scenarios/complaint.py)
- 问题: ScenarioFSM.can_transition / execute_transition / get_allowed_transitions and the three concrete FSMs (RefundScenarioFSM, OrderInquiryScenarioFSM, ComplaintScenarioFSM) are never invoked on any runtime path. set_scenarios() (orchestrator.py:113) is never called by app wiring; no strategy ever emits a 'switch_scenario' action (grep shows it only appears in the types Literal and the orchestrator's match arm); and the switch_scenario arm (orchestrator.py:714-743) bypasses the FSM machinery anyway — it sets context.fsm_state = scenario.get_initial_state() directly without validating transitions. e
- 影响: The user asked where the code can still be improved. This is a sizeable, fully-tested-but-dead subsystem (3 files + the FSM base) that adds maintenance and cognitive cost while contributing nothing to behavior — and the one place that does touch scenarios (switch_scenario arm) doesn't use the transition guards/actions, so the FSM's safety value is unrealized even if wired. Comparable to the render
- 建议: Either wire it (have a strategy emit switch_scenario and drive turns through execute_transition so guards/actions run) or remove the unused FSM classes and the switch_scenario arm. If kept intentionally as a §-spec utility like renderers, document it explicitly so it isn't mistaken for live code.
- 复核: VERIFIED REAL. The scenario FSM subsystem is entirely unwired at runtime, exactly as claimed.

(1) set_scenarios() defined at orchestrator.py:113 is NEVER called: `grep -rn "\.set_scenarios("` across src/ and tests/ returns nothing. The onl…

**MEDIUM-3. DB save deletes and re-inserts the entire history every turn (O(N) write amplification)**  _(performance)_
- 位置: `src/open_chat_shop/storage/db_context.py` @ 292-310
- 问题: Every save() does a full reconcile: SELECT all non-meta rows, DELETE every one (loop at 298-299), then INSERT the entire context.history again (loop at 301-310) plus the assistant row. So a session with H history messages performs H deletes + (H+1) inserts on every single turn, all inside asyncio.to_thread. This is O(N) DB writes per message where O(1) (append the new turns only) is achievable.
- 影响: Write volume grows linearly with conversation length: the 50th turn rewrites ~100 rows just to add 2. Combined with the unbounded-history finding above, a long session degrades into hundreds of row deletes+inserts per message. The delete-all-then-reinsert pattern is also why the duplicate-assistant bug is easy to introduce. The docstring justifies this as 'compaction shrinks storage', but compacti
- 建议: Switch to incremental append: persist only the new user turn + assistant turn each save (the rows not already present), and run the prune/delete branch only when compress() actually trimmed history (compare a stored high-water count or the oldest in-memory timestamp). This makes the steady-state cost O(1) per turn instead of O(N).
- 复核: REAL but HIGH is overstated → MEDIUM. The described behavior is verified exactly. db_context.py:292-299 SELECTs every non-meta row for the session and deletes them one-by-one (`for row in old_rows: db.delete(row)` — ORM per-row DELETE, no b…

**MEDIUM-4. Order-mutation tools duplicate the entire get_for_user → status-guard → snapshot/mutate/restore scaffold**  _(simplification)_
- 位置: `src/open_chat_shop/tools/builtin/cancel_order.py, modify_address.py, create_refund.py, check_refund_eligibility.py` @ cancel_order.py:40-80, modify_address.py:41-87, create_refund.py:47-80
- 问题: The four order-scoped tools repeat the same body almost verbatim: pre_check() does `order = self._order_repo.get_for_user(order_id, context.user_id)` then `if order is None: CheckResult(passed=False, '订单 X 不存在')` then a status guard; execute() RE-FETCHES the same order, RE-CHECKS None, RE-CHECKS the same status guard (so the order_id→user ownership lookup and status validation each run twice per call), then the three writers all do save_snapshot(order_id) → mutate → and define an identical compensate() of `self._order_repo.restore_snapshot(params['order_id'])`. cancel_order and modify_address 
- 影响: This is the single largest copy-paste surface in the tool layer. Beyond the line count, the double fetch+guard is a real correctness smell: pre_check and execute can disagree if state changes between them, and a new order tool must re-derive the ownership/None/snapshot ritual by hand (easy to forget the get_for_user user-scoping that the audit just hardened). A small `OrderScopedTool(BaseTool)` mi
- 建议: Add an `OrderScopedTool` base (or a `_load_owned_order` helper + a default snapshot `compensate`) in tools/builtin and have cancel_order/modify_address/create_refund/check_refund_eligibility/query_order/query_logistics derive the order lookup + not-found message from it; have execute() reuse the pre_check'd order instead of re-fetching.
- 复核: REAL duplication, but the finding overstates impact (HIGH) and some specifics are inaccurate/unsafe. Verified facts:

(1) Double fetch+guard is real. orchestrator.py:794 runs `tool.pre_check` then :808 runs `tool.execute` back-to-back in on…

**MEDIUM-5. _session_messages cap-trim block copy-pasted 4× in the customer WebSocket handler**  _(simplification)_
- 位置: `src/open_chat_shop/api/app.py` @ 477-480, 484-487, 495-498, 531-539
- 问题: The append-then-trim idiom `msgs = _session_messages.setdefault(session_id, []); msgs.append({...}); if len(msgs) > _msg_history_cap: _session_messages[session_id] = msgs[-_msg_history_cap:]` is repeated four times inside websocket_chat (HUMAN_MODE forward, TRANSFER_PENDING, normal user record, and assistant record). The assistant copy (531-539) re-reads _session_messages[session_id] right after appending to it.
- 影响: Four copies of a bounded-append is error-prone (the cap is enforced in 4 places that must stay in sync) and inflates a function that is already very long. A one-line helper removes the duplication and centralizes the 200-cap.
- 建议: Add a closure `_append_session_message(sid, entry)` near _msg_history_cap that appends and trims once, and call it at all four sites.
- 复核: VERIFIED REAL. The append-then-trim idiom is genuinely duplicated 4× inside websocket_chat (src/open_chat_shop/api/app.py): sites at 477-480 (HUMAN_MODE), 484-487 (TRANSFER_PENDING), 495-498 (normal user record), and 531-539 (assistant reco…

**MEDIUM-6. CascadeStrategy provider cascade defined but never wired (dead abstraction)**  _(architecture)_
- 位置: `src/open_chat_shop/core/provider.py` @ 78-133
- 问题: CascadeStrategy (multi-provider failover with tool/streaming/vision degradation, ~55 lines) has zero references in src/ outside its own definition (grep confirms). The real wiring in main.py build_orchestrator (lines 345-362) instead selects a SINGLE provider (_build_provider tries Anthropic then LiteLLM at construction time) and wraps only its `.chat` with _resilient_chat. So the system's headline 'cascade strategy' (advertised in the module docstring and CascadeStrategy.__doc__) does not actually run; provider failover is construction-time, not per-request.
- 影响: It is misleading dead code that implies a runtime capability the deployment does not have, and it is the natural seam for the multi-provider extensibility this project advertises. Either it should be the real provider entry point or it should be removed so the abstraction map matches reality.
- 建议: Decide the architecture: either (a) make CascadeStrategy the orchestrator's provider (wrap each configured provider, pass the list, and let it fail over + degrade per request — this is the more valuable design and matches the docstring), or (b) delete CascadeStrategy and its embed() twin and drop the 'cascade' language from the module docstring. Do not leave it as advertised-but-unwired.
- 复核: VERIFIED REAL (dead abstraction), but with corrected facts and a severity lean toward the low end.

Core claim confirmed. `grep -rn CascadeStrategy src/` returns exactly one hit: its own definition at src/open_chat_shop/core/provider.py:78.…

**MEDIUM-7. CircuitBreaker HALF_OPEN→OPEN reopen and probe-limit branches are live in production (main.py wiring) but never exercised by tests**  _(tests_coverage)_
- 位置: `src/open_chat_shop/core/resilience.py` @ 73-76 (probe-limit reject) and 104-110 (HALF_OPEN failing probe reopens); tests/unit/test_resilience.py only covers HALF_OPEN→CLOSED at 77-92
- 问题: main.py:353-362 wraps provider.chat as retry_policy.execute(circuit_breaker.call, _call) for every LLM call, so the CircuitBreaker state machine is on the hot path. But the tests only cover the happy transitions: test_resilience.py:77-92 trips OPEN then proves a SUCCESSFUL probe closes the circuit. There is NO test for (a) a FAILING probe in HALF_OPEN reopening the circuit (resilience.py:104-110, the `if self._state == CircuitState.HALF_OPEN: self._state = OPEN` branch), nor (b) the probe-limit rejection when half_open_calls >= half_open_max (resilience.py:73-76 raising CircuitOpenError). grep
- 影响: Coverage gap on resilience+circuit-breaker, called out explicitly by the dimension. The branches are reachable in production and protect against hammering a recovering-but-still-failing LLM endpoint, yet a regression that broke the reopen logic (e.g. forgetting to set OPEN on a failed probe → flapping/closed prematurely) would pass all tests.
- 建议: Add two CircuitBreaker tests: (1) trip OPEN, sleep past recovery_timeout, make the probe FAIL, assert state returns to OPEN and the failure_count behavior; (2) in HALF_OPEN with half_open_max=1, issue a second concurrent/sequential call and assert CircuitOpenError('probe limit reached').
- 复核: REAL and worthwhile. Coverage objectively confirms the gap: running tests/unit/test_resilience.py + test_audit_provider.py + test_main.py with --cov=open_chat_shop.core.resilience reports 97% with Missing = lines 74, 105-106 — exactly the t…


### LOW (10)

**LOW-1. Stale context slots and internal pending dicts leak into the response-cache key**  _(correctness_deep)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 345-349, 395-399 (with intent.py:285-309)
- 问题: _enrich_with_context merges ALL context.slots into intent.entities (intent.py:291-292), including a stale order_id from a prior query and internal control dicts (_pending_action, _pending_confirmation, _clarifying_response). The cache lookup/store then build params = dict(intent.entities) (orchestrator.py:345, 395) and hash that into the cache key (cache.py:54). Strategy._schema_params correctly strips these before the tool call, but the cache key is built from the raw enriched entities, not the schema-filtered set.
- 影响: A read-only search_product / query_order cache key gets polluted by leftover slots and underscore-prefixed pending state, so logically-identical queries produce different keys (cache fragmentation / near-zero hit rate) and pending-action churn keeps invalidating reuse. Not a correctness or security issue (now user-scoped), purely a hit-rate/efficiency loss — hence LOW.
- 建议: Build the cache params from the same schema-filtered / non-underscore projection used for tool params (reuse strategy._schema_params semantics or drop _-prefixed keys before hashing), so the cache key reflects only the request-relevant entities.
- 复核: LOW — not adversarially verified…

**LOW-2. Default RBAC grants 'agent' and 'admin' roles tools:['*'] while only customer-facing auth is enforced on those endpoints**  _(security_residual)_
- 位置: `src/open_chat_shop/core/security.py` @ 232-234
- 问题: _DEFAULT_RBAC maps agent and admin to {"*"} (PermissionChecker.has_permission short-circuits to True for any tool). context.user_role defaults to "customer" (context.py:108, db_context loads meta.get("user_role","customer")), and there is no code path in the request flow that elevates user_role from the verified JWT/claims — so today the wildcard roles are unreachable from the chat path and the gate is effectively customer-only. That is safe now, but the wildcard is a latent escalation surface: if/when user_role ever becomes settable from session metadata (the DB meta row already round-trips u
- 影响: The audit reshaped the RBAC YAML so PermissionChecker parses it, but the role itself is never derived from a verified claim — so the layer cannot actually distinguish privilege today, and the '*' grants would become dangerous the moment role assignment is wired without provenance checks.
- 建议: Bind context.user_role from a trusted source (JWT claim) the same way user_id is bound, and avoid persisting/trusting user_role from client-influenced session meta. Consider replacing '*' with explicit per-role tool lists so a future role-assignment bug cannot grant blanket access.
- 复核: LOW — not adversarially verified…

**LOW-3. Cache path computes two MD5 hashes + two entities dict copies per turn**  _(performance)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 344-399
- 问题: _core_handle builds params = dict(intent.entities) and calls _make_key (json.dumps(sort_keys=True)+md5, cache.py:54) once on the get (line 347) and again, with a freshly rebuilt dict(intent.entities), on the set (line 395). For every non-error, non-cached read-only turn this is two JSON serializations + two MD5 digests + two dict copies over the same logical key.
- 影响: Pure redundant hot-path work on every turn that reaches the cache (search_product/query_order/query_logistics). MD5 over a JSON blob plus dict copies is cheap individually but is paid twice per request and is trivially avoidable. Minor, but it is dead duplication introduced by the user-scoped cache wiring.
- 建议: Compute the params dict and the cache key once near intent classification and reuse it for both the get and the set, instead of rebuilding params and re-hashing. Optionally cache the key on the Intent or a local variable.
- 复核: LOW — not adversarially verified…

**LOW-4. Pending-action / topic-switch path runs the rule matcher twice over the same message**  _(performance)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 310
- 问题: When context.slots has a _pending_action, _core_handle calls self._intent_engine._rule_matcher.match(message.content) (line 310) to detect a topic switch. If it is a topic switch (or _try_resolve_pending returns None), control falls through to classify() at line 339, whose Level-1 step calls _rule_matcher.match(message.content) again — the identical regex scan over all registered rule patterns. The same full scan runs twice for the same input.
- 影响: Doubles the Level-1 regex work (scan of every intent's compiled patterns, plus _extract_entities) for the subset of turns that carry a pending action. Low impact since rule sets are small, but it is redundant work added by the topic-switch guard reaching into the engine's private _rule_matcher.
- 建议: Have classify() accept an optional precomputed rule_result, or cache the last match keyed by message identity, so the topic-switch probe and Level-1 share a single match() call. Reaching into _intent_engine._rule_matcher also couples the orchestrator to engine internals — a small public hook (e.g. quick_match) would be cleaner.
- 复核: LOW — not adversarially verified…

**LOW-5. _record_turn+save duplicated at 4 sites (miss → silent history drop)**  _(simplification)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 300-301, 330-331, 353-354, 403-404
- 问题: Every successful exit from _core_handle repeats the exact pair `self._record_turn(context, message, <resp>)` immediately followed by `await self._context_manager.save(context, <resp>)` — at the pending-confirmation return (300-301), the pending-action return (330-331), the cache-hit return (353-354), and the normal return (403-404). The post-_record_turn-then-save sequence is the load-bearing 'persist this turn' step the audit added.
- 影响: Four hand-maintained copies of the persist-and-record sequence is exactly the shape where a future early-return forgets one half and history/context silently stops persisting on that path (the audit's whole MEDIUM fix was 'nothing else appends to history'). A single `await self._finalize_turn(context, message, resp)` helper that does record+save makes every exit consistent by construction and is i
- 建议: Add `async def _finalize_turn(self, context, message, response)` that calls _record_turn then await save, and replace all four record+save pairs (and the cache-hit cast) with a single call.
- 复核: REAL and not yet handled. Verified in src/open_chat_shop/core/orchestrator.py: the exact two-line pair `self._record_turn(context, message, <resp>)` then `await self._context_manager.save(context, <resp>)` is repeated at all four successful…

**LOW-6. _execute_action's per-case `_llm_enhance → if not None return → else build AgentMessage` is repeated across 6 branches**  _(simplification)_
- 位置: `src/open_chat_shop/core/orchestrator.py` @ 617-746
- 问题: The match in _execute_action repeats the same shape in the reply/confirm/clarify/transfer/end/switch_scenario arms: call `llm_msg = await self._llm_enhance(action, context)`, `if llm_msg is not None: return llm_msg`, otherwise construct an AgentMessage from action.payload with a per-type message_type and text_fallback key. That's six copies of the enhance-or-template dance (618-650 alone is three back-to-back).
- 影响: The branches differ only in (message_type, the payload key used for text_fallback, and a couple of flags like requires_confirmation). The repeated try-LLM-then-template plumbing obscures those small differences and makes the method ~130 lines. A tiny `_enhanced_or(action, context, message_type, fallback_key, **flags)` helper collapses the common path and leaves each case as one line, making the ge
- 建议: Introduce `async def _reply_with_llm(self, action, context, *, message_type, fallback_key, requires_confirmation=False)` and rewrite reply/confirm/clarify/end (and the switch_scenario fallback) as single calls; leave transfer's queue/handoff logic inline since it is genuinely different.
- 复核: LOW — not adversarially verified…

**LOW-7. main.py build_orchestrator is a 200-line wiring monolith — no composition-root/DI seam, mixes backend selection + resilience + middleware assembly**  _(architecture)_
- 位置: `main.py` @ 228-432 (build_orchestrator)
- 问题: build_orchestrator does everything in one function: YAML loading, security config translation, context-backend selection, intent registration + sample loading, provider selection, tool registry + routing-rule construction, resilience monkey-patching of provider.chat (357-362), observability wiring, scenario/handoff registration, dual Redis client construction with inline rationale, middleware pipeline assembly, and response-cache wiring. There is no seam to construct a variant (e.g. tests that want a real provider but in-memory everything else, or a second channel set) without copy-pasting thi
- 影响: This is the de-facto composition root, but it is neither testable nor composable. Adding a provider/channel/backend means editing this monolith. The monkey-patch of provider.chat is a fragile abstraction leak (it mutates the provider object rather than decorating at the call boundary), and 'immutability / no mutation' is a stated project rule.
- 建议: Split into small builder functions returning typed parts (build_context_manager, build_intent_engine, build_tools, build_resilient_provider, build_middleware) and have build_orchestrator compose them. Replace the provider.chat monkey-patch with an explicit ResilientProvider decorator class implementing LLMProvider that wraps chat/stream/embed, so resilience is a first-class object, not in-place mu
- 复核: Partially real, but the premise is materially inaccurate and the severity is overstated. Downgrading MEDIUM -> LOW.

WHAT'S TRUE (the valid kernel): main.py:362 `provider.chat = _resilient_chat` is a genuine in-place mutation of the provide…

**LOW-8. Provider resilience test reproduces main.py's wiring by hand-copy rather than exercising the real build_orchestrator wrapping (drift risk)**  _(tests_coverage)_
- 位置: `tests/unit/test_audit_provider.py` @ 74-87 (_wire_resilient_chat, docstring: 'Reproduce main.py's resilience wiring verbatim'); real wiring at main.py:353-362
- 问题: The strongest retry test (test_wrapped_transient_failure_is_retried_then_succeeds, lines 107-133) and its permanent-error sibling rely on _wire_resilient_chat, a test-local helper that re-implements the retry_policy.execute(circuit_breaker.call, _call) wrapping by COPY. Its own docstring says it reproduces main.py verbatim. Because it does not import/exercise main.build_orchestrator's actual wrapping, the two can drift: if someone changes the order to circuit_breaker.call(retry_policy.execute,...), tweaks max_retries, or removes the wrapping in main.py, these tests keep passing against the sta
- 影响: The dimension targets over-mocking / tests that exercise a stand-in instead of the real path. A verbatim hand-copy of production wiring is a classic divergence trap; the test asserts a behavior of the copy, not of main.py.
- 建议: Have the test obtain the wrapped provider from the real wiring — e.g. call main.build_orchestrator() (with a stubbed _build_provider returning the flaky client) and assert orchestrator._provider.chat retries — or factor the wrapping in main.py into a small importable helper (wrap_resilient(provider)) that both main.py and the test call, eliminating the copy.
- 复核: LOW — not adversarially verified…

**LOW-9. No integration test for orchestrator+DatabaseContextManager (hid the double-persist)**  _(tests_coverage)_
- 位置: `tests/integration/test_pipeline.py` @ 40-46 (build_orchestrator uses InMemoryContextManager); tests/e2e/test_chat_flow.py:28-37 likewise
- 问题: Both the integration suite (test_pipeline.py) and e2e suite (test_chat_flow.py) build the orchestrator with InMemoryContextManager + MockProvider. The just-remediated DatabaseContextManager (history reconciliation, to_thread offload, write-through get cache, cumulative token meta — the +222-line STORAGE fix) and RedisContextManager are only covered by unit tests using sqlite:///:memory: with StaticPool (test_db_context.py, test_audit_storage.py) and a FakeRedis fake (test_redis_context.py, test_audit_storage.py FakeRedis). So the orchestrator↔real-backend seam (load→handle→save→reload across a
- 影响: The dimension flags 'paths only hit with Mock/InMemory (the real DB/Redis backends).' The storage layer got the single largest remediation but has zero integration coverage with the orchestrator driving it — exactly where a save/load reconciliation regression would surface in production but not in tests.
- 建议: Add one integration test that wires DatabaseContextManager(sqlite file) (and, gated/skippable, RedisContextManager against fakeredis or a real redis) into build_orchestrator, drives 2-3 turns through handle_message, and asserts history/token_usage/mode round-trip correctly across an explicit reload — covering the orchestrator+backend interaction, not just the backend in isolation.
- 复核: LOW — not adversarially verified…

**LOW-10. Attack zero-tool security gate is only proven in the rule-only (provider=None) path; LLM-function-calling attack routing is untested**  _(tests_coverage)_
- 位置: `tests/unit/test_audit_tests.py` @ 170-198 (forces main._build_provider = lambda: None) and tests/unit/test_attack_blocking.py:32-43 (no provider wired)
- 问题: Both attack-neutralization suites assert the security property only with the LLM disabled. test_audit_tests.py:170 mutates the module global main._build_provider to None (rule-only routing) before asserting summarize_attack_security.all_neutralized; test_attack_blocking.py builds an orchestrator with no provider at all. The CLI security gate (__main__.py:104-118) likewise reports the zero-tool guarantee, but in keyless CI the LLM path never runs. So the deterministic guarantee 'no attack reaches a tool' is verified for the rule/intent-fallback path (3 blocked by injection detector, 5 by intent
- 影响: The dimension asks about over-reliance on Mock paths and whether the security-eval truly has teeth. The gate's teeth are real but scoped to rule-only routing; the LLM-function-calling attack surface (the actual production risk vector) has no negative test. This is a coverage boundary worth making explicit rather than a definite bug — hence LOW.
- 建议: Add a test with a MockProvider scripted to RETURN a tool_use/tool_call for an injection input, and assert the orchestrator still does not execute a business tool for an attack sample (i.e. the post-LLM guard, not just intent fallback, holds). Also prefer monkeypatch.setattr over raw `main._build_provider = ...` assignment for safe isolation.
- 复核: LOW — not adversarially verified…
